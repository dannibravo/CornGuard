# CornGuard ML pipeline dependencies. Pin exact versions once a real training run happens on a
# specific machine/CUDA setup — left as minimum versions for now since this pipeline has never
# been executed (see README.md).
#
# Requires Python 3.9-3.12 — as of this writing, tensorflow has no build for Python 3.13+, so a
# dedicated virtualenv on an older interpreter is needed on machines (like this one) that default
# to a newer Python.
tensorflow>=2.15,<2.17
numpy>=1.26,<2.0
pandas>=2.1
scikit-learn>=1.3
matplotlib>=3.8
Pillow>=10.0
